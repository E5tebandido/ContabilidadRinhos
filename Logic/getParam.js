var delContent = () => {
    if (document.getElementById (
        "panel"
        )
    ) {
        document.getElementById (
            "panel"
        ).innerHTML = "";
    }
}

var pdata = () => {
    delContent();
}

var createTableForm = () => {
    
}

var operationForm = () => {

}

