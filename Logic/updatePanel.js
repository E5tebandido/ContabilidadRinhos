document.addEventListener (
    "DOMContentLoaded", 
    (event) => {
        createNav();
    }
);

var userPanel = () => {
    udata("rdata","athletes");
}

var paymentPanel = () => {
    udata("rdata","payment");
}